import React, { Component } from 'react';
import { Animated, View } from 'react-native';

export default class App extends Component {
  constructor(props) {
    super(props)
    this.excitedAnimation = new Animated.Value(0)
  }
}

excitedNerdling = () => {
  Animation.stagger
}

Animated.delay(
  this.state.
)